{
	"name": "ddoz",
	"version": "1.0.0",
	"private": true,
	"cloudflare": {
		"bindings": {
			"DECOMPRESSED_SIZE_IN_GIB": {
				"description": "Controls the size of data produced during decompression in GiB (default: 1 GiB)."
			}
		}
	},
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.42.2"
	}
}
